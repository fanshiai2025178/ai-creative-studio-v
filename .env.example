# ===================================
# AI Creative Studio V2 - 环境变量配置
# ===================================

# -----------------------------
# 数据库配置
# -----------------------------
# MySQL 连接字符串格式：mysql://用户名:密码@主机:端口/数据库名
# 示例：DATABASE_URL="mysql://root:yourpassword@localhost:3306/ai_comic"
DATABASE_URL="mysql://username:password@localhost:3306/database_name"

# -----------------------------
# JWT 密钥（用户认证）
# -----------------------------
# 生成方式：node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="your-super-secret-jwt-key-change-this"

# -----------------------------
# Google Gemini AI API
# -----------------------------
# ⚠️ 重要：系统不再配置默认 API Key
# 用户必须在注册时填写自己的 Gemini API Key（存储在数据库）
# 获取地址：https://makersuite.google.com/app/apikey
# GEMINI_API_KEY 已废弃，不要配置此项

# Gemini API 代理地址（大陆服务器必须配置）
# 如果服务器在大陆，无法直接访问 Google API，需要配置代理
# 推荐使用 gemini-balance-lite 部署在美国 VPS 上
# 示例：GEMINI_PROXY_URL="http://your-us-vps-ip:3001"
GEMINI_PROXY_URL=""

# -----------------------------
# 本地存储配置（图片存储）
# -----------------------------
# 网站的基础 URL，用于生成图片访问链接
# 本地开发：http://localhost:3000
# 生产环境：http://fansai.online
BASE_URL="http://localhost:3000"

# -----------------------------
# 服务器配置
# -----------------------------
# 服务器运行端口
PORT=3000

# Node 环境（development 或 production）
NODE_ENV=development

# -----------------------------
# 可选：Manus 相关配置（如果还在使用）
# -----------------------------
# 应用 ID（如果完全独立部署可删除）
# VITE_APP_ID=""

# OAuth 服务器地址（如果完全独立部署可删除）
# OAUTH_SERVER_URL=""

# 所有者 OpenID（如果完全独立部署可删除）
# OWNER_OPEN_ID=""
